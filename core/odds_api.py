"""
core/odds_api.py — TOMBSTONE — quota épuisé, source définitivement supprimée.
Source remplacée par : Harvester (1XBet direct) + Gemini/Pinnacle (guerrilla mode).
DO NOT IMPORT — kept only to avoid import errors in legacy branches.
"""


def fetch_odds(*args, **kwargs):
    """Dead — use core.harvester.fetch_matches() + core.harvester.fetch_pinnacle_prices()."""
    return []
